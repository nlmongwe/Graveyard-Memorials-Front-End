import 'package:flutter/material.dart';

class FindAGraveMobileView extends StatelessWidget {
  const FindAGraveMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Find a grave mobile"),
    );
  }
}
