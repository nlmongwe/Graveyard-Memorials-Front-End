import 'package:flutter/material.dart';

class FindAGraveDesktopView extends StatelessWidget {
  const FindAGraveDesktopView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Find a grave desktop view"),);
  }
}
